package NVocal;

message TClientState {
    optional bytes ServerPublicKey = 1;
    optional bytes PublicKey = 2;
    optional bytes PrivateKey = 3;
    optional string Login = 4;
    optional string Host = 5;
    optional bytes SessionKey = 6;
}
